numpy
pandas
# Base NI TDMS reader; optional HDF/pandas extras are intentionally excluded.
nptdms==1.11.0
PyQt5
asammdf
av
pyqtgraph>=0.13.3
openpyxl
pytest>=7.0
pytest-qt>=4.2
qtawesome

# BLF (Vector CAN log) import — BLFReader is pure-Python file parsing (no HW);
# cantools decodes frames into named signals via a DBC. Cross-platform.
python-can==4.6.1
pya2ldb==1.0.332  # A2L 解析（import 名是 pya2l）— frozen/source contract pin
cantools>=39

# Stage 8 - Vector/XCP backend (Windows-only)
python-can[vector]==4.6.1; sys_platform == "win32"
pyxcp==0.29.14; sys_platform == "win32"
