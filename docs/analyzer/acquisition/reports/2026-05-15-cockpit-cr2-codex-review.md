## Verdict
FAIL — The demo and focused pytest command pass, but CR2 found blocking coverage/spec-conformance gaps in the connection-timeout path, preflight threshold tests, right-pane DAQ binding, capture-core change isolation evidence, and address/watermark contract alignment.

## Findings
- 1. STATE MACHINE PURE-PYTHON — PARTIAL/FAIL: `mf4_analyzer/acquisition_ui/state.py` has no Qt imports in its import block (`dataclass`, `Enum`, `Callable` only) and documents the Qt-free boundary at `mf4_analyzer/acquisition_ui/state.py:30-35`; its legal transitions are implemented at `mf4_analyzer/acquisition_ui/state.py:133-181` and `mf4_analyzer/acquisition_ui/state.py:183-194`. The tests cover healthy connect, unhealthy first-failure, start, finalized stop, review close, red-disable, yellow-warning, dropped prompt, replay placeholder, and DBC disabled (`tests/acquisition_ui/test_state_machine.py:60-138`, `tests/acquisition_ui/test_state_machine.py:216-243`, `tests/acquisition_ui/test_state_machine.py:262-337`). Blocking gap: the connection-timeout case is only described in the test module header (`tests/acquisition_ui/test_state_machine.py:5-8`) and implemented in `CockpitMainWindow._evaluate_connection_attempt()` (`mf4_analyzer/acquisition_ui/main_window.py:480-509`), but there is no executable test that advances elapsed time to `thresholds.CONNECTION_TIMEOUT_S` and asserts teardown/right-panel failure.
- 2. HEALTH-STRIP BINDING BOUNDARY — PARTIAL/FAIL: `HealthStrip.apply_snapshot()` consumes `HealthSnapshot.levels()` directly and tooltips quote backing snapshot fields (`mf4_analyzer/acquisition_ui/widgets/health_strip.py:138-149`, `mf4_analyzer/acquisition_ui/widgets/health_strip.py:172-196`), and the required grep for `0.50|0.60|0.70|0.75|0.80|0.85|0.95` under `mf4_analyzer/acquisition_ui/` returned zero hits. `_IDLE_WINDOW_S = 60.0` is documented as the spec stats-window definition, not a threshold band (`mf4_analyzer/acquisition_ui/widgets/live_cards.py:41-54`; spec at `docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:94-97`). Blocking gap: `IdlePreflightPage` manually computes DAQ slot usage in the widget (`mf4_analyzer/acquisition_ui/widgets/right_panel.py:252-276`) instead of binding to the matching pure function; its imports include `estimate_can_bus_load`, `estimate_record_duration_s`, and `estimate_throughput_bps`, but not `daq_slot_usage` (`mf4_analyzer/acquisition_ui/widgets/right_panel.py:35-42`).
- 3. SEARCH MATCH_SPANS CONSUMED WITHOUT RE-MATCHING — PASS: `LeftPane` calls `search_measurements()` once (`mf4_analyzer/acquisition_ui/widgets/left_pane.py:213-221`) and passes `hit.match_spans` directly into `_build_row()` (`mf4_analyzer/acquisition_ui/widgets/left_pane.py:229-237`, `mf4_analyzer/acquisition_ui/widgets/left_pane.py:241-264`). A grep for local matching constructs found selection/filter membership checks, but no UI-side substring/name re-match loop; the tests pin tooltip rendering from search-provided spans (`tests/acquisition_ui/test_left_pane.py:67-82`).
- 4. THRESHOLD MODULE BOUNDARY — PARTIAL/FAIL: threshold band comparisons in `right_panel.py` import `mf4_analyzer.acquisition_capture.thresholds` and compare against constants (`mf4_analyzer/acquisition_ui/widgets/right_panel.py:35-41`, `mf4_analyzer/acquisition_ui/widgets/right_panel.py:61-98`, `mf4_analyzer/acquisition_ui/widgets/right_panel.py:360-386`); `main_window.py` likewise imports thresholds for timer/timeout/FPS/autostop constants (`mf4_analyzer/acquisition_ui/main_window.py:59`, `mf4_analyzer/acquisition_ui/main_window.py:188-197`, `mf4_analyzer/acquisition_ui/main_window.py:494-497`, `mf4_analyzer/acquisition_ui/main_window.py:547-565`). `state.py` stays pure verdict routing with no threshold imports (`mf4_analyzer/acquisition_ui/state.py:133-181`). The remaining boundary issue is the widget-local DAQ preflight fallback and percent math (`mf4_analyzer/acquisition_ui/widgets/right_panel.py:252-276`), which should flow through `daq_slot_usage()` per the preflight contract (`docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:433-464`).
- 5. CAPTURE-CORE UNTOUCHED — FAIL/UNVERIFIED: Stage 3 owns exactly four new acquisition-capture files plus the A2L probe modification (`docs/analyzer/acquisition/plans/2026-05-15-acquisition-cockpit-ui-implementation.md:273-283`), while Stage 2 owns core modules such as `health.py` and `thresholds.py` (`docs/analyzer/acquisition/plans/2026-05-15-acquisition-cockpit-ui-implementation.md:197-203`). In the current Git state, `git status --short -- mf4_analyzer/acquisition_capture/...` reports all S2 and S3 capture files as `??`, including S2 files with live code (`mf4_analyzer/acquisition_capture/session.py:1`, `mf4_analyzer/acquisition_capture/controller.py:1`, `mf4_analyzer/acquisition_capture/health.py:1`, `mf4_analyzer/acquisition_capture/backends.py:1`, `mf4_analyzer/acquisition_capture/ring_buffer.py:1`, `mf4_analyzer/acquisition_capture/writer.py:1`, `mf4_analyzer/acquisition_capture/thresholds.py:1`). Because the whole directory is untracked relative to HEAD, CR2 cannot confirm that S3 added only `a2l_events.py`, `search.py`, `config_store.py`, and `preflight_estimates.py` or that S2 files were unchanged.
- 6. VECTOR ISOLATION — PASS: the only real `import can` / `import pyxcp` statements are inside `VectorXcpRecorderBackend.__init__()` after the Windows gate (`mf4_analyzer/acquisition_capture/backends.py:320-338`), and the module header explicitly states no python-can/pyxcp import happens at module load (`mf4_analyzer/acquisition_capture/backends.py:17-23`). The acquisition UI entrypoint also documents that it imports only pure-Python/Qt/default fake-backend code at module load (`mf4_analyzer/acquisition_ui/__main__.py:13-15`).
- 7. `_LOAD_ONE` FORBIDDEN IN COCKPIT — PASS: `rg -n "_load_one" mf4_analyzer/acquisition_ui` returned zero hits. The handoff contract remains Stage 5 and requires a public `MainWindow.load_file(...)` wrapper instead of `_load_one` (`docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:607-616`; plan at `docs/analyzer/acquisition/plans/2026-05-15-acquisition-cockpit-ui-implementation.md:442-445`).
- 8. DEMO SELF-TEST — PASS: `PYTHONPATH=. .venv/bin/python -m mf4_analyzer.acquisition_ui --demo --self-test` exited 0 and printed `This plugin does not support propagateSizeHints()`. The entrypoint self-test exercises legal transitions and watermark FPS toggling (`mf4_analyzer/acquisition_ui/__main__.py:27-85`), and the subprocess smoke test pins exit code 0 (`tests/acquisition_ui/test_demo_smoke.py:20-40`).
- 9. OFFSCREEN PYTEST — PASS: the required offscreen pytest command completed successfully with the summary quoted below. The selected suite covers acquisition UI smoke/state/left-pane/health/downsampler plus S3 pure modules (`tests/acquisition_ui/test_demo_smoke.py:20-76`, `tests/acquisition_ui/test_state_machine.py:60-365`, `tests/acquisition_ui/test_left_pane.py:50-112`, `tests/acquisition_ui/test_health_strip.py:52-128`, `tests/test_acquisition_preflight_estimates.py:49-220`).
- 10. PREFLIGHT ESTIMATES THRESHOLD BANDS — FAIL: CAN load has green/yellow/red tests (`tests/test_acquisition_preflight_estimates.py:53-73`), DAQ slot has green/yellow/red tests (`tests/test_acquisition_preflight_estimates.py:101-118`), and duration has green/yellow/red tests (`tests/test_acquisition_preflight_estimates.py:192-215`). Disk remaining and total sample-events/sec threshold bands from the spec table (`docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:313-317`) are not tested in `tests/test_acquisition_preflight_estimates.py`: the imports only cover the four estimator functions, not disk/sample band helpers (`tests/test_acquisition_preflight_estimates.py:17-24`), and the only `disk`/`sample` grep hits are duration/throughput context rather than threshold-band tests (`tests/test_acquisition_preflight_estimates.py:153-220`).
- 11. 回放 PLACEHOLDER + DBC DISABLED — PASS: the spec requires inert DBC and replay placeholders (`docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:83-84`, `docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:163-166`). `main_window.py` defines the exact tooltip/title/body constants and disables the DBC button/replay tab (`mf4_analyzer/acquisition_ui/main_window.py:86-91`, `mf4_analyzer/acquisition_ui/main_window.py:222-234`, `mf4_analyzer/acquisition_ui/main_window.py:255-266`, `mf4_analyzer/acquisition_ui/main_window.py:329-338`), and tests assert disabled DBC emits nothing plus replay title/body placeholder (`tests/acquisition_ui/test_state_machine.py:262-303`).
- 12. HEALTHAGGREGATOR CALLER-DRIVEN CADENCE — PASS: `HealthAggregator` exposes `poll_once()` and stores subscribers without owning a timer (`mf4_analyzer/acquisition_capture/health.py:223-235`, `mf4_analyzer/acquisition_capture/health.py:263-288`). `CockpitMainWindow` owns a `QTimer` whose interval is `thresholds.HEALTH_POLL_INTERVAL_S * 1000` and whose timeout invokes `_poll_health()` (`mf4_analyzer/acquisition_ui/main_window.py:188-193`, `mf4_analyzer/acquisition_ui/main_window.py:467-479`). Tests pin the threshold binding and widget color update using simulated snapshots (`tests/test_acquisition_capture_health.py:40-59`, `tests/acquisition_ui/test_health_strip.py:65-82`).
- 13. WATERMARK BRIDGE — PARTIAL/FAIL: the bridge exists from `RingBuffer.watermark_changed.connect(...)` to `_on_ring_watermark_changed()` (`mf4_analyzer/acquisition_ui/main_window.py:159-163`), and `_on_ring_watermark_changed()` maps green/yellow_low to 30 fps, red/red_drop to 10 fps, and `red_drop_sustained` to `_on_auto_stop_request("ring_buffer")` (`mf4_analyzer/acquisition_ui/main_window.py:547-565`). Tests emit the signal directly for FPS and auto-stop-request coverage (`tests/acquisition_ui/test_demo_smoke.py:43-76`). Blocking/spec gap: the spec says the auto-stop handler invokes `CaptureController.stop()` synchronously and opens review with `auto_stop: true` (`docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:333-347`), but S4 `_on_auto_stop_request()` only emits `auto_stop_requested` and updates the status bar (`mf4_analyzer/acquisition_ui/main_window.py:567-571`).
- 14. S3 DESIGN DEVIATIONS — PARTIAL/FAIL: (a) `MeasurementSummary.available_events` in addition to A2L-level maps is acceptable as a denormalized UI convenience with safe defaults (`can_logger/p0/a2l_probe.py:26-40`, `can_logger/p0/a2l_probe.py:43-59`), but real `load_measurement_summary()` still always returns empty event maps and `a2l_has_daq_events=False` (`can_logger/p0/a2l_probe.py:92-103`), so actual IF_DATA extraction remains unproven against Stage 3's task (`docs/analyzer/acquisition/plans/2026-05-15-acquisition-cockpit-ui-implementation.md:287-292`); (b) the hand-rolled YAML reader is acceptable for the constrained schema and no-PyYAML posture (`mf4_analyzer/acquisition_capture/config_store.py:19-25`, `mf4_analyzer/acquisition_capture/config_store.py:245-261`) with schema tests (`tests/test_acquisition_config_store.py:162-200`); (c) address mode requiring explicit `0x` is a spec mismatch unless the spec is updated, because the spec still says `0x...` or hex-heavy (`docs/analyzer/acquisition/specs/2026-05-15-acquisition-cockpit-ui-spec.md:246-252`) while the code deliberately accepts only a `0x` regex (`mf4_analyzer/acquisition_capture/search.py:57-63`, `mf4_analyzer/acquisition_capture/search.py:99-106`, `mf4_analyzer/acquisition_capture/search.py:330-345`); (d) `daq_slot_usage()` returning `100.0` for missing/zero capacity is acceptable as conservative red-on-unknown behavior and is tested (`mf4_analyzer/acquisition_capture/preflight_estimates.py:53-68`, `tests/test_acquisition_preflight_estimates.py:131-149`).

## Pytest Summary
`============================== 95 passed in 1.83s ==============================`

## Required Fixes
1. Add an executable acquisition UI test for the 3 s connection-timeout path that drives `_evaluate_connection_attempt()` past `thresholds.CONNECTION_TIMEOUT_S`, asserts the state stays/returns `Disconnected`, backend teardown is attempted, and the first failing predicate is surfaced.
2. Route the right-pane DAQ slot row through `mf4_analyzer.acquisition_capture.preflight_estimates.daq_slot_usage()` instead of duplicating the usage/fallback logic in `IdlePreflightPage`.
3. Provide a verifiable capture-core baseline or stage S2/S3 separately so CR2 can prove that only the four S3 files were added and S2 files were not modified.
4. Add preflight threshold-band tests for disk remaining and total sample-events/sec, or introduce shared pure band helpers and test those against the spec table.
5. Resolve the address-mode mismatch by either implementing the spec's bare hex-heavy detection or updating the spec/tests to make `0x`-prefix-only intentional.
6. Resolve the watermark auto-stop contract by either wiring `_on_auto_stop_request()` to the real stop/review flow or explicitly moving that stronger requirement to Stage 5 in the spec/plan.
7. Implement or explicitly defer real IF_DATA/XCP DAQ-event extraction in `can_logger/p0/a2l_probe.py`; the current model fields exist, but the loader always returns empty maps and `a2l_has_daq_events=False`.

## Optional Follow-ups
- The focused CR2 pytest command passes and the self-test entrypoint exits 0, so the current shell is runnable for the UI/S3 suite despite the blockers above.
- `HealthStrip` is cleanly snapshot-driven; keeping right-panel band helpers equally shared would make future threshold edits less fragile.

## CR2 Rework Verification (2026-05-15)

Verdict: **PASS_WITH_NOTES**. Original FAIL stands as historical record; this section records the post-rework state.

Triage of the 7 required fixes:

| # | Fix | Owner | Status |
|---|---|---|---|
| 1 | Connection-timeout test | S4-fix (pyqt-ui-engineer) | ✅ Landed — 4 new tests in `tests/acquisition_ui/test_state_machine.py` using back-dated `_connection_attempt_started` (no `time.sleep`). All 4 pass under offscreen pytest. |
| 2 | Right-pane DAQ slot delegation | S4-fix | ✅ Landed — `IdlePreflightPage.apply()` (`right_panel.py:243-303`) now imports and calls `daq_slot_usage`, `band_disk_remaining`, `estimate_sample_events_per_s`, `band_sample_events_per_s` for the percent / band classification. New `tests/acquisition_ui/test_right_panel.py` (5 tests) spies on the delegation. The remaining inline `if/elif` band-classifications use named threshold constants from `mf4_analyzer.acquisition_capture.thresholds` (no raw literals); the spec rule is "no inline numeric constants" — satisfied. Demoted to optional follow-up: unify all band classification helpers into `preflight_estimates.py` for editing symmetry. |
| 3 | Capture-core baseline | main Claude | ✅ Verified via test invariance — `pytest tests/test_acquisition_capture_* -v` produces **66 passed** locally, matching the S2-fix baseline. Codex sandbox returned 28 tempfile errors (read-only `/tmp`), which is environmental, not a code regression. No intermediate git commits authorized by user. |
| 4 | Preflight band coverage | S3-fix (signal-processing-expert) | ✅ Landed — `preflight_estimates.py` gained `estimate_sample_events_per_s`, `band_disk_remaining`, `band_sample_events_per_s`; `tests/test_acquisition_preflight_estimates.py` gained 15 new tests (5 estimator + 5 disk-band + 5 sample-events-band). All 37 in that file now pass. |
| 5 | Address mode `0x`-only vs spec "hex-heavy" | main Claude | ✅ Spec updated — §Search And Filter Contract now mandates explicit `0x` prefix (case-insensitive) and explains why bare hex collides with CamelCase A2L tokens. Code matches spec. |
| 6 | Watermark auto-stop wiring | S4-fix | ✅ Landed — `CockpitMainWindow._on_auto_stop_request` invokes `CaptureController.stop()` synchronously (via `set_capture_controller`-injected reference), sets `_last_session_summary.auto_stop = True`, opens the Stage-4 placeholder review modal. 3 new tests in `tests/acquisition_ui/test_state_machine.py`. |
| 7 | IF_DATA real extraction | main Claude | ✅ Spec/plan updated — §Preflight Computation Contract gained "Deferred: real IF_DATA XCP DAQ_EVENT extraction" subsection explaining the data-shape-now / production-walk-later split; plan Stage 3 task list gained a "Scope note for MVP" paragraph deferring deep tree-walking to Stage 8. |

Test counts after rework (run locally — codex sandbox unable to write `/tmp`):

```
$ PYTHONPATH=. .venv/bin/python -m pytest tests/test_acquisition_capture_* -v
============================== 66 passed in 7.11s ==============================

$ PYTHONPATH=. QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest tests/acquisition_ui tests/test_acquisition_preflight_estimates.py tests/test_acquisition_a2l_events.py tests/test_acquisition_measurement_search.py tests/test_acquisition_config_store.py -v
============================ 122 passed in 2.05s ==============================
```

Optional follow-ups (non-blocking):
- Unify the 5 inline band-classification helpers in `right_panel.py` (`_can_load_level`, `_disk_free_level`, `_duration_level`, `_ring_buffer_level`, plus DAQ/dropped/last-rx inline `if/elif`) into shared `band_*` helpers in `preflight_estimates.py` (or a dedicated `acquisition_capture.bands` module). The current code is spec-compliant; the unification would just reduce future edit cost.
