# post-v8.2 hardening · Cocoa 前台验收记录

- 执行时段：2026-09-03 23:53:26 至 2026-09-04 00:04 CST（UTC+08:00）
- 主机：macOS 27.0 (26A5425a), arm64, Apple M5, Studio Display 5120×2880 Retina
- Qt 平台：`cocoa`；前台探针的 primary screen 为 2560×1440 logical、DPR `2.0`、72 DPI。
- 初始源码快照：`HEAD 0253cdcc25734631a5021bb9bbc9a60226a7cad0`
  (`fix(ui): settle chart option x-range envelopes`)；`git status --porcelain` SHA-1
  `7d77961d2c9a48e59564fd644ec6a4f1a02a7dd7`，非验收文档 diff SHA-1
  `09fa2dcac0f45851ffc0b2ab8c750f9da7671b6f`。
- 结束快照（00:10）：同一 `HEAD`；并行 lanes 在执行中继续改动，porcelain SHA-1 变为
  `a65d5de323a236339cb5bd4d510d1bbf46fea587`，非验收文档 diff SHA-1
  变为 `c13b88756c29bde6bafff0583275f47b9cfbb28e`。因此本记录只为每个明确列出的
  前台证据背书，不能作为该脏树其余行为的通过结论。
- 证据目录（未纳入 Git）：`.state/t11-cocoa-2026-09-03/`。

本记录只包含真机前台 Cocoa 操作；没有以 `QT_QPA_PLATFORM=offscreen` 或单元测试
替代任何未完成项。

## 结果

| 项目 | 状态 | 真机操作与观察 | 证据 / 阻塞 |
| --- | --- | --- | --- |
| C3 应用菜单 Quit | **PASS** | 在前台 TraceLab 中新增 View 7 产生未保存变更；打开 macOS `Python` 应用菜单并点 `Quit Python`。出现“项目有未保存的更改。是否保存？”守卫，含“保存 / 不保存 / 取消”；点“取消”后窗口和未保存状态仍在。 | `02-application-menu-quit-guard.jpeg`。菜单标题为 Python 是开发解释器的应用名，不是产品名。C3 没有绕过，故未改 `command_coordinator.py` / `app.py`，也未新增 `QEvent.Quit` 红测。 |
| C3 Dock 退出 | **UNVERIFIED** | 未以应用菜单 Quit 代替 Dock 路径。Computer Use 对 `Dock` 的前台状态请求超时（`-10005`），随后 app 列表未暴露可操作的 Dock 目标。 | 需解锁可访问的 Dock 前台自动化或由人工在同一脏项目点击 Dock → Quit 后确认守卫。 |
| C3 注销 | **UNVERIFIED** | 未执行 macOS 注销；它会中断用户当前桌面会话，不能作为普通 UI 验收的安全动作。 | 需用户在隔离会话中手验，确认守卫而非退出。 |
| C2/C4 Control+Tab，QuickRef 搜索焦点 | **UNVERIFIED** | 可访问窗口中先选定 View 6，打开操作速查，焦点为“搜索操作…”，发送物理 `Control+Tab`；关闭 QuickRef 后仍显示 View 6。 | `03-quickref-control-tab.jpeg` 记录 QuickRef 前台焦点。该 UI 进程在本轮前已启动；执行中当前树又发生并行改动，不能将此观察定性为当前集成快照的产品失败。需要在可唯一定位的、加载数据的最新 Cocoa 窗口重跑。 |
| C2/C4 Control+Tab，时域 Canvas 焦点 | **UNVERIFIED** | 以 View 6 为基准，点击时域 Canvas 后发送物理 `Control+Tab`，可访问 UI 仍为 View 6。 | 同上：不是对最终整合树的可归因失败结论，须重跑。 |
| C2/C4 Control+Tab，通道树焦点 | **UNVERIFIED** | 可访问窗口的文件数为 0，通道树控件均 disabled，无法取得真正的 channel-tree focus。 | 初次生成加载数据窗口时，旧的 `_stop_smart_layout_timers` 调用链曾令 `open_project(testdoc/1.tlproj)` 失败；T3 后该错误不再复现（见 00:09 重试）。但 Mac 随即锁屏，Computer Use 不能把焦点切到新候选窗口。另起 WWT-loaded Python 进程时也受同一 Python app bundle 的窗口定位限制。 |
| D1 色标 × 双击 + 1 px 抖动 | **UNVERIFIED** | 未执行真实关闭：该动作会通过 GUI 删除当前 dirty session 中的 View。 | Computer Use 对 GUI 本地删除要求行动时确认；本轮未获该确认。不可用离屏事件/host mock 替代。 |
| E2 光标设置弹层下 pill 避让 | **UNVERIFIED** | 无可定位的加载曲线窗口，因而没有实际 dual-cursor pill 可供观察。 | T3 后 project-open 候选已启动，但锁屏阻断其前台聚焦；并有同 bundle 窗口定位限制。不可只验弹层开关而宣称 pill 避让通过。 |
| View overflow `⋯` 入口四边 | **PASS（仅 `⋯` 形态）** | 7 View 的 Retina 前台截图中，常驻“管理全部 View”入口可见，`⋯` 小矩形外框四边连续、没有左右边缺失。 | `01-dirty-seven-views.jpeg`。该宽度未产生隐藏 tab，故 `»N` 形态仍为 **UNVERIFIED**；未为了制造 overflow 继续修改当前共享 dirty session。 |
| View overflow popup 本体 | **UNVERIFIED** | 点击常驻入口后，`Qt.Popup` 未能保留到 Computer Use 的下一次可访问性快照；没有把空白/关闭状态误当作 frame 通过。 | 需能稳定保持 popup 的前台屏幕取证，再检查 header/body/footer 四边。 |

## 可复查截图

- `.state/t11-cocoa-2026-09-03/00-initial-foreground.jpeg`：初始前台窗口。
- `.state/t11-cocoa-2026-09-03/01-dirty-seven-views.jpeg`：新增 View 7 后的脏态前台窗口，含常驻 `⋯` 入口。
- `.state/t11-cocoa-2026-09-03/02-application-menu-quit-guard.jpeg`：C3 通过的原生菜单 Quit 守卫。
- `.state/t11-cocoa-2026-09-03/03-quickref-control-tab.jpeg`：QuickRef 搜索框持有焦点时的快捷键尝试。

## 结论与后续

C3 的**应用菜单 Quit** 路径已由真机守卫证实，当前不存在批准的 C3 修复条件。T11
尚不能关闭：Dock / 注销、三处 Control+Tab、D1、E2、`»N` 与 popup frame 均保留为
**UNVERIFIED**。

### 00:09 重试（T3 之后）

T3 完成后，当前树已不再含 `_stop_smart_layout_timers` 引用。以
`QT_QPA_PLATFORM=cocoa` 启动独立 Python/TraceLab 进程并调用
`open_project(testdoc/1.tlproj)`：启动输出仅有既有 HDF 的
`RuntimeWarning: invalid value encountered in cast`，进程 `15585` 在 00:10 仍存活，
未复现旧的 `AttributeError`。这说明 loaded-project 前台候选已成功创建；它不是
Control+Tab 或 pill 的替代性验收。

随后的 Computer Use 前台窗口切换返回：`The Mac is locked and automatic unlock could
not unlock it.`。没有屏幕解锁便无法确认该候选窗口为活动窗口、取得通道树/Canvas 焦点、
或观察 pill / popup。因此这次重试没有新增 PASS 或 FAIL，表内未完成项继续为
**UNVERIFIED**；没有运行 offscreen 替代项。
