# Pin 布局与闪退分析

2026-09-19；基于当前 `a5c10c79` 的已有脏工作区、用户截图、系统崩溃报告与独立诊断进程。此次没有修改应用源码。HTML 是交互原型，不是 Qt 验收。

## Findings

### P1：Pin 引导线坐标映射可直接导致原生崩溃

`mf4_analyzer/ui/pg_canvas/pinned_cursor_overlay.py:1211`、`:1214` 用 `canvas.mapTo(glw, point)` 从父控件映射到后代控件。当前 Qt5 运行时中，此方向可在原生 `QWidget::mapTo` 中触发 SIGSEGV；外围 Python `try/except` 无法捕获。对象存活检查不足以保护错误的映射关系。

最新用户操作时段的系统报告为 `~/Library/Logs/DiagnosticReports/Python-2026-09-19-164137.ips`，进程从 16:39:18 运行到 16:41:35，主线程 `QWidget::mapTo` → PyQt 包装 → Python 回调，调用链下方有 `QTimer::timerEvent`，访问地址 `0x28`。Pin 的几何定时器在 `pinned_cursor_overlay.py:634` 连接 `_reproject_geometry`，后者调用 `_sync_leaders`。

隔离诊断 `.state/probe_pin_mapto.py` 构造真实父/子 Qt 控件，直接调用当前生产 `_sync_leaders`，结果：

```text
ancestor: True
Fatal Python error: Segmentation fault
.../pinned_cursor_overlay.py, line 1211 in _sync_leaders
exit code: 139
```

安全方向对照：`glw.viewport().mapFrom(canvas, point)` 再传入 `glw.mapToScene`，同环境坐标往返为 True，退出 0。后续修复需同时遵守 QGraphicsView 的 viewport 坐标域，而非只调换 mapTo/mapFrom 字样。

通常 Pin 标签因聚集、贴边、排布位移而生成 leader，进而进入该分支（`:317`）。这说明故障可能在缩放、标签拥挤时出现，不一定每次 Pin 都立即出现。尚未复现用户原始文件和完整前台操作；系统报告没有 Python 源行，所以“该缺陷可崩溃”已证实，“这次事故必然由此行触发”仍是高度吻合的判断。

同类危险调用还在 `chart_stack/pinned_cursor_controller.py:790`、`:794`、`:795` 的频谱命中检查里；此次未单独调用该生产分支。当天 10:36、11:28 的既有报告也以 mapTo/0x28 开头；12:16 的报告则是 Python 退出时 `sip_api_visit_wrappers`，不能合并为同一个根因。本次诊断新产生的崩溃记录不计入用户历史事故。

建议下一步先修坐标映射与 focused subprocess regression，再做 Cocoa 下密集 Pin、边界标签、缩放和频谱命中验收。此报告没有执行修复。

### P2：继承浮动面板位置，加标题避让，必然容易堆叠

`chart_stack/pinned_cursor_controller.py:1021` 起，新 Pin 直接继承 live pill 的 x/y；`:1230` 起 `_nudge_live_from_pins` 仅对已固定面板最多 26px 高的标题矩形做避让，而不是整个面板矩形。避让对象是 live pill，并非统一重新布局所有 Pin。

因此并不是代码写死“屏幕正中央”，而是连续 Pin 复制同一浮动位置后，仅移动少量距离。用户截图的阶梯式重叠与该逻辑一致。手动已摆放的 live pill 会跳过自动避让。

`:1191` 的 anchor 恢复只做边界约束，没有固定面板之间的碰撞解算。`cursor_pill.py:32` 的白背景 alpha 为 235/255，会让下层内容透出少量，放大重叠时的视觉干扰；但截图中的所有浅色文本不能仅凭此常量归因。

### P2：默认竖线对比过低

`pg_canvas/pinned_cursor_overlay.py:40` 起，single pin 为 `#7090be`，默认 alpha 110/255；`:1032` 默认宽度 1.0，虚线。白底上混合后约为 `#c1cfe3`，与浅色网格区分不足。高亮时才变为 alpha 220、宽度 1.5。

建议默认清晰蓝灰 `#54749d` / 1.5px 虚线，选中 `#006bea` / 2.5px 实线，编号与面板同步高亮。通过线型、粗细、编号共同区分，不仅靠颜色；通道继续保留原有颜色。双游标仍需保留 A/B 端点语义，本轮原型仅演示单点 Pin。

## 第一轮方案（已退回，不作为执行依据）

用户指出：当前左右面板已经占满，右侧是横坐标、时间范围和滤波设置，不能假定那里有可增加停靠区的空间。第一轮原型缺失完整三栏背景，因此撤回“右侧停靠优先建议”；也不以收起现有 Inspector 为前提。

原型：`../ui-prototypes/2026-09-19-pin-layout-options.html`。

| 方案 | 新 Pin 去向 | 多 Pin | 主要代价 |
| --- | --- | --- | --- |
| A 右侧停靠（已否决） | 图表内固定读数列表，按时间排序 | 滚动；列表和竖线联动 | 当前布局没有额外侧栏空间 |
| B 底部对比 | 对比表新增一行，通道共享列 | 数值横向对齐；纵向滚动 | 降低绘图区高度，多通道需横向滚动 |
| C 就近单卡 | 对应竖线附近，只展开选中 Pin | 其他只保留编号；编号条选择 | 不能同时展开多张；仍有局部遮挡 |

共同规则：Pin 绑定数据坐标，编号删除后不复用，视野外保留记录并标明状态；面板内容用不透明底。原型包含方案切换、密集 8 点、线条强弱对照、拖动/方向键调整 Pin、双击新增、删除、范围缩放；使用合成数据，不读取用户数据。

## 第二轮方案：完整三栏内比较，待用户选择

原型：`../ui-prototypes/2026-09-19-pin-in-chart-options.html`。本轮开始时 HEAD 为 `c75c47e3`，重新查看当前文件确认上述危险 mapTo 调用仍存在；未重复触发崩溃，也未修改应用源码。

按用户截图重建完整界面背景：左侧文件和通道、中央工具栏和 View 标签、右侧横坐标/时间范围/滤波。左右栏保持原有占用，所有 Pin 方案都在中央区域内分配空间。外框为非交互布局参照；波形和读数是合成数据，不是运行中 Qt 应用。

| 方案 | 面板逻辑 | 空间不足 | 明确代价 |
| --- | --- | --- | --- |
| A 图内分散浮卡 | 完整卡片碰撞检测，靠近各 Pin；可拖标题，连线关联 | 保留编号，点击后优先展开；长列表内部滚动 | 遮挡局部曲线；无法无限同时展开 |
| B 顶部紧凑对比带 | 通道名一份，每个 Pin 一列，替代图例区域 | 水平滚动 Pin 列；多通道纵向滚动 | 占中央约 116–166px 高度 |
| C 轻量标记展开 | 默认两项简短读数；选中一个展开完整详情 | 紧凑卡也放不下时保留编号；用上方索引选中 | 完整通道信息需要点击；仍有局部遮挡 |

共同规则：竖线增强对比，通道颜色保留；密集轴标签合并为可轮换的组，所有 Pin 仍可在上方编号索引中访问；详情为不透明白底。支持截图 3 Pin、密集 8 Pin、2/6 长名称通道、强弱线对比、视野缩放、增删、卡片拖动和收起。这里展示候选行为，尚未批准为产品交互契约。落地时仍须明确自动排布稳定性、用户拖动优先级、分屏和双游标语义。

## 第三轮：用户指定 A 方案的底部按钮交互

在同一新版 demo 中更新 A，B/C 保留作参考，尚未改应用源码：

- 所有初始/新增 Pin 默认收进底部各自的 P 按钮。每个 Pin 的展开状态独立，点击 P1 不影响 P2/P3；支持同时展开多张面板。
- 面板上的“−”只收起对应面板；“×”删除该 Pin；“全部收起”收起所有面板。重新排布不擅自展开面板。
- 底部 P 按钮横向拖动调整其数据时间，竖线、标题时间和读数同步更新。拖动保留当前展开状态；点击与拖动以 3px 阈值区分。
- Demo 提供 Shift 拖动的 1/10 速度精调和左右键每次 1ms 调整。这是用于评估的候选精调参数，实际产品还需依据采样/吸附契约确定，不在此把合成连续波形当作实测采样。
- 密集位置保留每个可拖动 P 按钮，标签按行错开；不再只显示不可独立拖动的聚合入口。编号跨越其他 Pin 后保持不变。多卡按完整矩形避让，长内容内部滚动。
- 两侧面板占用保持；闪退事项仍保留在下节，等用户决定后一并写入执行 plan。

## 后续 plan 必须带入的闪退事项

用户要求先记好原因，选定 UI 后再统一写 plan。本节为待纳入内容，不表示当前已批准实现。

1. **优先级 P1，先做独立修复任务。** Owner 为 `pg_canvas/pinned_cursor_overlay.py` 的 leader 投影和 `chart_stack/pinned_cursor_controller.py` 的频谱命中转换；共享的 mapTo 方向错误应一起覆盖。
2. **证据保留。** 系统报告 `Python-2026-09-19-164137.ips`、生产 `_sync_leaders` 在 1211 行的隔离进程 exit 139、viewport `mapFrom` 对照 exit 0；旧行号在执行时重新核对。最小复现脚本 `.state/probe_pin_mapto.py` 是临时证据，执行时应把必要场景转成持久回归测试。
3. **修复约束。** QWidget 祖先/后代映射方向必须正确，QGraphicsView.mapToScene 输入必须属于 viewport 坐标域；不以 try/except 或 sip.isdeleted 替代坐标契约。
4. **先红后绿。** 原生崩溃用独立子进程测试并断言正常退出及正确坐标。覆盖密集标签、贴边标签、非零 viewport 偏移和频谱命中；避免直接让 pytest 主进程触发 SIGSEGV。
5. **Focused gates。** `tests/ui/test_pinned_cursor_geometry.py`、`test_pinned_cursor_interaction.py`、`test_pinned_cursor_capture.py`；如改 timer/teardown，增加 `test_pinned_cursor_lifecycle.py`；如触碰 collaborator 声明，增加 `test_pg_canvas_backref_invariants.py`。按实际变更选边界，不跑泛化全 UI suite。
6. **前台验收。** Cocoa 下创建多个 Pin、密集/边界标签、缩放/平移、切到频谱并命中；原始事故完整操作仍待复现。12:16 的 SIP 退出清理崩溃是另一类，单独调查，不混称修复完成。

## 验证边界

- 原生崩溃诊断：独立 offscreen 子进程生产方法复现 exit 139；安全坐标映射对照 exit 0。
- 本轮未做产品修复，因此不运行完整应用测试套件，也不宣称修复通过。
- 浏览器检查仅验证 HTML 原型；不替代 Qt 前台、macOS/Windows 产品验收。
- Chromium 实际检查：1440×1000 下 A/B/C 截图，密集 8 点、切换方案、缩放、添加、方向键和鼠标拖动；390×844 下页面无横向溢出。截图位于 `.state/pin-layout-{a,b,c,mobile}.png`。这些检查不构成所有边界条件验收。
- 第二轮 Chromium 几何检查：1600/1280px 宽 × 三种方案 × 2/6 通道 × 3/8 Pin，共 24 组。可见浮卡/紧凑卡均无相互重叠、无越过绘图区边界；同一窗口下左右栏宽度不随方案变化。1600px 时左右栏为 319/311px，1280px 时为 253/247px（clientWidth）。空间不够时确实减少展开数，未通过覆盖卡片或隐藏右栏获得通过。图像证据为 `.state/pin-v2-{scatter,matrix,compact}.png` 与 `.state/pin-v2-small-{scatter,matrix,compact}.png`；检查不证明浮卡遮挡曲线的程度可被接受。
- 第二轮交互检查：拖动卡片标题后位置改变，新增后编号数 4、删除后 3；缩放显示 P2/P3 视野外；选 P2 后完整卡片切到 P2，收起后完整卡片数 0；密集标签轮换到 P2；选择 P8 后顶部对比带水平滚动 324px。均为浏览器实际操作。
- 第三轮浏览器检查：A 初始 0 张，依次点 P1/P2/P3 为 1/2/3 张；拖 P1 后仍为 3 张，标题与状态时间同步；右方向键（ArrowRight）从 22.9162s 到 22.9172s；收起 P2 后 2 张，点 P1 收起后 1 张，新增 P4 后仍 1 张且 P4 aria-expanded=false。截图为 `.state/pin-v3-collapsed.png`、`.state/pin-v3-expanded.png`。
- 第三轮拖动区分检查：收起状态下拖动仍为 0 张；同样 40px 拖动，普通移动约 2.005s，Shift 移动约 0.201s（显示四位小数的逐帧舍入误差）。密集 8 个底部按钮无相互重叠，保留独立拖动入口。
- 第三轮密集展开检查：1280×1000 下依次展开 8 张，自动重排后卡片之间及卡片与底部按钮之间均无矩形重叠；高密度卡片内容须内部滚动。证据 `.state/pin-v3-dense.png`。这仅验证原型几何，不是 Qt 实现验收。

## 最新决定：正式优化计划（覆盖上述候选交互）

已编写 [Pin 底部按钮、多面板与闪退修复计划](../plans/2026-09-19-pin-bottom-handles-and-crash-fix-plan.md)，本次只改文档，未实施产品修复。

- 用户最后截图中的整条额外 Pin 工具栏不保留：数量/说明、上方编号索引、“＋ Pin”“全部收起”均删除，不留空白行、不移到右侧；创建沿用现有 P 键。
- 采用 A 的默认收进底部 Pn、横向微调、多个面板独立展开。生产原有标题 ＋/− 已用于 full/mini，计划保留其含义，展开/收起统一由底部 Pn 控制；这覆盖第三轮 demo 的“− 收起”候选语义。
- 使用平台拖动阈值，不照搬 demo 的 3px。明确预览/一次提交/取消、双点端点、坐标域、持久化和捕获边界。
- 密集展开的几何无重叠不能证明文字可读；不得照搬将内容压成半行的行为。至少常规窗口三卡可读，极小空间保留请求展开意图并明确空间不足。
- 原生 `mapTo` 崩溃证据已进入计划 T1，先修已复现缺陷，再做拖动。原始事故完整重放、独立 SIP 清理崩溃仍保持未解决/未验证标识。
- 当前 HTML 仍是第三轮候选快照，尚未按此次删工具栏决定修改；正式计划优先，后续实施按 T5 同步 demo。
