# Plan Review Rev 2 — 2026-04-27-batch-blocks-redesign

Verdict: approved with minor revisions

## Finding Status (F1..F9)

- **F1 — resolved.** Rev 1 identified the UI-thread full-load dry-run risk through W6 calling `_expand_tasks` and W2 resolving `file_paths` via the loader (rev-1 lines 12, 47-48); rev 2 W6 now requires `_build_dry_run_preview()` to use UI state only, pull disk rows from cached `FileListWidget` probe data, and never call `BatchRunner._expand_tasks` because that would `loader(path)` on the UI thread (rev-2 lines 1744-1756).
- **F2 — resolved.** Rev 1 found that all disk-load failures could collapse to zero tasks and a generic blocked run (rev-1 lines 14, 48); rev 2 makes `_LoadFailure` count as a phase-1 yield candidate, yields it through phase 2, raises it inside `run()`, and emits `task_failed` in the exception path (rev-2 lines 739-760, 841-874).
- **F3 — resolved.** Rev 1 found `progress_callback` fired after failed tasks (rev-1 lines 16, 39); rev 2 states the callback fires once per `task_done` after `on_event(task_done)`, adds a failed-task callback-count test, and places the callback immediately after the `task_done` event inside the success branch only (rev-2 lines 321-324, 496-514, 849-860, 861-874).
- **F4 — resolved.** Rev 1 found `tests/ui/test_order_smoke.py` still patched the deleted `batch_sheet` module (rev-1 lines 18, 36); rev 2 explicitly adds `tests/ui/test_order_smoke.py:66` to W4 and changes the patch target to `mf4_analyzer.ui.drawers.batch.BatchSheet` (rev-2 lines 1166-1172, 1374-1380).
- **F5 — resolved.** Rev 1 found UI unlock depended on `finished_with_result` instead of guaranteed `QThread.finished` (rev-1 lines 20, 48); rev 2 says `BatchRunnerThread.run()` wraps crashes into a blocked result, and `BatchSheet` unlocks from Qt's built-in `finished` signal while `finished_with_result` only stores the result (rev-2 lines 1730-1738, 1763-1767, 1911-1927).
- **F6 — resolved.** Rev 1 found the task-list/progress UI underspecified for output count, progress bar, and remaining-time behavior (rev-1 lines 22, 33); rev 2 defines the idle header as `N 任务待执行 · M 输出`, the running header as `进度 i/N [progress bar] ~Ts 剩余`, and the ETA formula, with tests for output count, progress text, and progress value (rev-2 lines 1720-1728, 1777-1787, 1790-1810).
- **F7 — partially resolved.** Rev 1 listed missing §8 coverage for multi-signal runner expansion, failed-task progress counts, cancellation completeness, SignalPicker collapse, file-state transitions, run-disabled-while-probing, TaskList collapse, cancel UI, and `apply_preset` states (rev-1 lines 24, 41-42); rev 2 adds runner tests for multi-signal expansion, failed-task callback count, all-disk-failed rows, and no-half-written cancellation, plus TaskList collapse and toolbar `apply_preset` tests (rev-2 lines 496-578, 537-552, 555-578, 1833-1843, 2020-2056). Residual gaps remain because W4 deletes old BatchSheet tests while replacement tests still do not cover PipelineStrip status recomputation from inputs, SignalPicker ESC/outside-click collapse, explicit `path_pending -> probing -> loaded`, run-disabled-while-probing, automated sheet-level cancel/unlock UI, or the 5GB probe smoke; the self-review also still says §8 tests are in Waves 1-6 even though W7 creates toolbar tests (rev-2 lines 1202-1210, 1529-1558, 1606-1615, 1851-1876, 1938-1941, 1995-2085, 2203).
- **F8 — partially resolved.** Rev 1 found W5 did not define the `BatchSheet`/panel accessors W7 consumed and W7 did not cover current-single time range restoration (rev-1 lines 26, 39); rev 2 adds a W5 public API contract for `method()`, `selected_signals()`, `rpm_channel()`, `time_range()`, `file_ids()`, `file_paths()`, outputs, and apply mutators, and W7 adds a current-single test that asserts `time_range()` restoration (rev-2 lines 1490-1517, 2020-2038). The fix is incomplete because W7 also calls `signals_marked_unavailable()` but W5's public API contract does not name that accessor (rev-2 lines 1493-1517, 2041-2056).
- **F9 — resolved.** Rev 1 asked the plan to clarify the intentional default-loader divergence from spec §4.3 (rev-1 lines 28, 39); rev 2 adds an explicit note that the actual repo exposes `DataLoader.load_mf4`, treats the spec text as adapted to that API, and shows `_default_loader()` returning `FileData` via `DataLoader.load_mf4` (rev-2 lines 654-667).

## New Findings

1. **[should-fix] W5/W7 API contract still mismatches on unavailable-signal inspection.** W5's public `BatchSheet` API lists state accessors and mutators but omits `signals_marked_unavailable()` (rev-2 lines 1493-1517), while the W7 unavailable-signal test calls `sheet.signals_marked_unavailable()` and asserts its tuple return (rev-2 lines 2041-2056). Add the accessor to W5's contract, or change W7 to assert unavailable marking through a named existing API (rev-2 lines 1493-1517, 2041-2056).
2. **[should-fix] W4 test pruning still relies on replacement coverage that is not fully specified.** W4 deletes the two pre-redesign `test_drawers.py` BatchSheet tests and says Waves 5-7 add comprehensive replacements (rev-2 lines 1382-1389), but the planned replacements cover only a direct `PipelineStrip.set_stage()` smoke, basic SignalPicker emit/search/partial states, a loose disk-add state assertion, a runner-thread smoke, and manual cancel verification (rev-2 lines 1202-1210, 1529-1558, 1606-1615, 1851-1876, 1938-1941). Add explicit tests for the remaining rev-1 §8 gaps before relying on that pruning: panel-driven PipelineStrip status transitions, ESC/outside-click picker collapse, `path_pending -> probing -> loaded`, run-disabled-while-probing, and sheet-level cancel/unlock UI (rev-1 lines 24, 41-42; rev-2 lines 1382-1389).

## Summary

Rev 2 resolves the rev-1 must-fix backend and threading problems: dry-run preview is metadata-only, `_LoadFailure` now reaches `task_failed`, and `progress_callback` is success-only after `on_event(task_done)` (rev-2 lines 1744-1756, 739-760, 849-860). The Wave 4 import-order issue and the QThread unlock contract are also explicitly corrected (rev-2 lines 1166-1172, 1763-1767). The plan still needs minor revisions for UI test coverage and one W5/W7 accessor mismatch before it is low-risk to execute (rev-2 lines 1493-1517, 2041-2056, 1382-1389). No new must-fix issue was found in the W2 phase-1/phase-2/yield path or the `progress_callback` placement cross-check (rev-2 lines 739-760, 849-874).
