# Prune Report — 2026-06-17

**Mode:** prune
**Run date:** 2026-06-17
**Staleness cutoff:** `updated` older than **2025-12-17** (6 months before run date)

## Method

1. Globbed `orchestrator/decompositions/*.md`, sorted descending by
   filename, took the most recent 20 (range
   `2026-05-16-acquisition-ui-overflow` … `2026-06-15-bottom-hint-bar-weighted-rotation`).
2. Grepped every `docs/lessons-learned/**/*.md` path referenced inside
   those 20 decomposition files → reference set (protected, never pruned).
3. Walked the full corpus across
   `{signal-processing, pyqt-ui, refactor, orchestrator}/*.md` and parsed
   each lesson's frontmatter `updated` field. Where `updated` was absent
   (`NONE`), the filename date prefix (`YYYY-MM-DD`) was used as the
   effective last-touched date.
4. Candidate = NOT in reference set AND effective `updated` strictly
   before the cutoff (2025-12-17).

## Result

**Total prune candidates: 0.**

No lesson in the corpus qualifies. The oldest effective `updated` date
across all 69 corpus lessons is **2026-04-22**
(`orchestrator/2026-04-22-task-tool-unavailable-blocks-dispatch.md` and
peers), which is roughly **2 months** inside the 6-month retention
window. The staleness gate (`updated < 2025-12-17`) excludes the entire
corpus before the reference-set filter is even applied.

## Counts

| Role | Lessons scanned | Candidates |
|---|---|---|
| signal-processing | 9 | 0 |
| pyqt-ui | 47 | 0 |
| refactor | 2 | 0 |
| orchestrator | 11 | 0 |
| **Total** | **69** | **0** |

## Candidates by role

- **signal-processing:** none
- **pyqt-ui:** none
- **refactor:** none
- **orchestrator:** none

## Notes

- Several lessons carry no frontmatter `updated:` field (e.g.
  `pyqt-ui/2026-04-25-cache-invalidation-event-conditional.md`,
  multiple `orchestrator/*` entries). These were scored by filename-date
  fallback; all such fallbacks landed in 2026-04/05/06, well inside the
  window, so the missing-frontmatter cases did not change the result.
  This is a corpus-hygiene observation, not a prune trigger — a future
  maintenance pass could backfill `updated:` frontmatter on those files.
- Reference set (protected) drawn from the top-20 decompositions spans
  orchestrator routing lessons, pyqt-ui rendering/layout lessons,
  signal-processing cache/perf lessons, plus several `codex-*` review
  lessons living at the corpus root (outside the four role dirs and thus
  outside the prune walk anyway).
- `last_prune_at` bump in `.state.yml` is main Claude's responsibility;
  this report does not touch state.
