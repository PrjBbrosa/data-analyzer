# Master Lessons Index

Format: `- [<slug>](<role>/YYYY-MM-DD-<slug>.md) [tag1][tag2] — one-line hook`

Write protocol: `docs/lessons-learned/README.md`.

## orchestrator

- [task-tool-unavailable-blocks-dispatch](orchestrator/2026-04-22-task-tool-unavailable-blocks-dispatch.md) [dispatch][tooling][architecture][planner-executor-split] — Task absence is EXPECTED, not a blocker; orchestrator plans, main Claude dispatches.
- [move-then-tighten-causes-cross-specialist-rework](orchestrator/2026-04-22-move-then-tighten-causes-cross-specialist-rework.md) [decomposition][rework][cross-specialist] — Splitting "create file body" and "tighten file imports" across two specialists causes file-level rework; fold mechanical metadata edits into the body creator's brief unless domain expertise is required.
- [refactor-then-ui-same-file-boundary-disjoint](orchestrator/2026-04-24-refactor-then-ui-same-file-boundary-disjoint.md) [rework][boundary-adjustment][cross-specialist] — Rework detection fires on refactor→ui sequential edits of the same file even when method scopes are disjoint; the fix is enumerating forbidden methods per brief, not disabling the rule.
- [parallel-same-file-drawer-task-collision](orchestrator/2026-04-24-parallel-same-file-drawer-task-collision.md) [decomposition][parallel-serialization] — Parallelising same-expert tasks that each touch the same shared file (even for 1-line edits) causes `git add` commit-collision races; serialize or bundle shared-file edits into a single specialist's brief.
- [parallel-mutators-share-git-index-even-disjoint-files](orchestrator/2026-06-11-parallel-mutators-share-git-index-even-disjoint-files.md) [decomposition][parallel-serialization][git-index][worktree] — Disjoint-file parallel mutators still contend on the shared git index/HEAD; serialize file-mutating subagents (reviews can still parallelize), or use worktree isolation; big integration tasks run solo.
- [silent-boundary-leak-bypasses-rework-detection](orchestrator/2026-04-25-silent-boundary-leak-bypasses-rework-detection.md) [boundary][reporting][reviewer-discipline] — files_changed records files not symbols; specialists must list symbols_touched and reviewers must grep forbidden symbols, otherwise silent overscope passes both rework detection and review.
- [codex-prompt-file-for-long-review](orchestrator/2026-04-25-codex-prompt-file-for-long-review.md) [codex][review][tooling][shell] — Long Codex prompts with shell syntax must use `--prompt-file`; add `--write` when the output contract includes report artifacts.
- [interactive-playground-unblocks-ui-alignment](orchestrator/2026-04-26-interactive-playground-unblocks-ui-alignment.md) [ui][alignment][playground][decision-velocity][brainstorming] — When ≥3 UI proposals differ on visual/feel rather than logic, write a one-shot HTML playground (project-QSS palette, per-proposal toggles, copy-out prompt) instead of more text round-trips.
- [return-type-change-needs-paired-callsite-update](orchestrator/2026-04-28-return-type-change-needs-paired-callsite-update.md) [decomposition][return-type][downstream-coupling][planning-gap] — A function-return-type widening (e.g. T → T | None) is a contract change; bundle the call-site None-guard with the contract change, do not split it across specialists and force a flag round-trip.
- [non-dsp-algorithmic-python-routes-to-signal-processing-expert](orchestrator/2026-05-15-non-dsp-algorithmic-python-routes-to-signal-processing-expert.md) [routing][decomposition][signal-processing-expert][refactor-architect][body-vs-shape] — Body-creating numeric/algorithmic/persistence work belongs to signal-processing-expert even when not signal-DSP; refactor-architect's scope is move/shim/import only and the agent correctly refuses body work.
- [ui-redesign-verb-missed-squad-trigger](orchestrator/2026-05-30-ui-redesign-verb-missed-squad-trigger.md) [routing][roster-gap][trigger-keywords] — UI verbs (重设计/优化/spec→实现) AND style/layout nouns (圆角/间距/面板/playground 落地) miss the trigger set; route substantive `.py` edits via Missed-triggers. Signal is "does it ask for source edits?", not the keyword.

## signal-processing

- [envelope-cache-bucket-width-quantization](signal-processing/2026-04-25-envelope-cache-bucket-width-quantization.md) [envelope][downsample][cache][viewport] — Quantize viewport-cache keys to one bucket width (span/pixel_width), not a fixed percentage; the quantum must come from the same constant that discretizes the result.
- [cache-consumer-must-be-grepped-not-just-surface](signal-processing/2026-04-25-cache-consumer-must-be-grepped-not-just-surface.md) [cache][hot-path][dead-code][audit] — A cache reachable + invalidatable but never read on the hot path is dead code; grep the uncached helper at the consumer end, not just the cache method on the producer end.
- [batched-fft-transient-buffers-dominate-chunk-budget](signal-processing/2026-04-26-batched-fft-transient-buffers-dominate-chunk-budget.md) [fft][batch][chunk][memory] — A batched FFT pipeline transiently holds 3-4× chunk-frames-bytes (work + complex spectra + abs); use in-place arithmetic and explicit `del` to keep peak memory inside a 4× headroom contract.
- [plan-verbatim-source-must-reconcile-with-recent-removals](signal-processing/2026-04-27-plan-verbatim-source-must-reconcile-with-recent-removals.md) [batch][dispatch][supported-methods][plan-staleness] — A `SUPPORTED_*` gate must be a strict subset of live dispatcher handlers; cross-check plan-verbatim enums against `git log` removals or a ghost value falls through to `else: raise`.
- [pathlib-text-io-needs-explicit-utf8-on-windows](signal-processing/2026-04-27-pathlib-text-io-needs-explicit-utf8-on-windows.md) [io][encoding][windows][json][cjk][locale] — `Path.write_text` / `read_text` use locale codec on Windows (cp936/cp1252); `ensure_ascii=False` is a UTF-8-only choice and requires a paired `encoding="utf-8"` to be coherent end-to-end.
- [branch-reached-is-not-behavior-correct](signal-processing/2026-05-19-branch-reached-is-not-behavior-correct.md) [testing][decoder][timestamp][dto][regression-gap][evidence-quality] — A decoder test asserting one-frame output proves only the branch executed; time-series correctness requires two frames at different input states with strictly different outputs (e.g. increasing timestamps).
- [component-speedup-does-not-imply-end-to-end-target](signal-processing/2026-05-28-component-speedup-does-not-imply-end-to-end-target.md) [perf][envelope][end-to-end][micro-bench][hot-path] — A 76× envelope micro-bench win left the pan P50 at parity; measure the END-TO-END target path, not just the stage you optimized, before claiming an SLA win.
- [cancel-gap-was-unwired-call-site-not-missing-poll](signal-processing/2026-06-11-cancel-gap-was-unwired-call-site-not-missing-poll.md) [cancel-token][plan-staleness][call-site-wiring] — Grep the target signature before implementing a "missing" hook; cancel gaps are often unwired call sites, and stale call-site comments propagate the false premise.
- [unrealized-viewbox-width-phantom-decimates-small-traces](signal-processing/2026-06-12-unrealized-viewbox-width-phantom-decimates-small-traces.md) [envelope][downsample][pixel-width][pyqtgraph][viewbox][preview] — An un-shown pyqtgraph ViewBox reports a phantom ~45 px width (not 0); gate envelope bucket count with a min-realized FLOOR + generous fallback so small traces pass through untouched.
- [static-image-writer-test-api-wider-than-plan](signal-processing/2026-06-20-static-image-writer-test-api-wider-than-plan.md) [batch][write-image][backward-compat][static-method][test-api-surface] — Static batch helpers are often called DIRECTLY in tests with the old payload type; grep direct call sites before narrowing a @staticmethod's accepted type or add a dual-accept isinstance branch.
- [inline-db-floor-drift-invisible-to-surface-grep](signal-processing/2026-06-21-inline-db-floor-drift-invisible-to-surface-grep.md) [dB][floor][convergence][helper][amplitude][semantic-drift] — 1e-12 vs tiny floor differ by >5000 dB but are indistinguishable on in-range data; use spy-based TDD (call-count monkeypatch) to verify helper delegation is wired at every call site.
- [dead-branch-test-locks-wrong-contract](signal-processing/2026-06-21-dead-branch-test-locks-wrong-contract.md) [heatmap][amplitude_db][dead-branch][clip][tdd][test-contract][display-only] — A test asserting the buggy clip result instead of the invariant (unclipped matrix) locks in the bug; grep branch tests for "assert bug" vs "assert contract" before deletion, then add a ValueError guard test as RED→GREEN proof.
- [phantom-field-promotion-requires-guard-test-update](signal-processing/2026-06-21-phantom-field-promotion-requires-guard-test-update.md) [order][cot][phantom-field][guard-test][compute-contract][tdd][consumption-map] — Promoting a carry-through field to a real compute input requires moving it in the consumption-map guard in the SAME commit; use two-input TDD (different time_res → different n_frames) to prove wiring is live, not just reachable.
- [cache-key-dataclass-field-binding-and-phantom-fields](signal-processing/2026-06-21-cache-key-dataclass-field-binding-and-phantom-fields.md) [cache-key][dataclass][compute-contract][display-param][phantom-field][guard-test][introspection] — Bind the cache key's field set to the compute `*Params` dataclass with a `dataclasses.fields`+`inspect.getsource` guard (display params like db_reference stay off both); a dataclass field is NOT proof compute reads it (COTParams.time_res/fs are carry-through phantoms).
- [fallback-branch-must-delegate-not-shadow-primary](signal-processing/2026-06-21-fallback-branch-must-delegate-not-shadow-primary.md) [cache-key][fallback][delegate][shadow-implementation][invalidation][unified-entry-point][dict-get-none-trap][auto-nfft] — A fallback key branch must call the primary function AND patch None-valued inputs first; dict.get(key, default) returns None when the key exists — the default only fires on a missing key. Never modify the primary body to accept None; patch only in the fallback call-site shallow copy.
- [scipy-namespace-stub-survives-uninstall](signal-processing/2026-06-21-scipy-namespace-stub-survives-uninstall.md) [scipy][numpy][window][dependency-removal][pip-uninstall] — `pip uninstall scipy` leaves a namespace stub: `import scipy` succeeds but `from scipy.signal import X` raises; verify removal via submodule import, not bare `import scipy`.
- [multirate-rpm-align-on-real-timestamps-not-synth-grid](signal-processing/2026-06-22-multirate-rpm-align-on-real-timestamps-not-synth-grid.md) [order][cot][rpm][multirate][resample][interp][time-base][eps][upsample] — Multi-rate motor-speed (1k) upsamples onto the signal axis (48k) via np.interp; pass REAL signal timestamps not the degenerate-fallback uniform grid, guard both axes strictly-increasing+finite (skip on fail), never downsample the wideband signal.
- [head-calibration-is-metadata-not-sample-gain](signal-processing/2026-06-22-head-calibration-is-metadata-not-sample-gain.md) [hdf][head][loader][calibration][metadata][scaling][eps][zero-multiply][test-locks-bug] — HEAD FLOAT32 samples are already physical; `calibration` is metadata, not a gain. `cal=0` zeroing real data is the smoking gun; grep `.field` sites for downstream consumers + fix the test that asserts the scaled (buggy) value.

## pyqt-ui

- [bottom-tick-fitter-reject-overfine-not-thin](pyqt-ui/2026-06-17-bottom-tick-fitter-reject-overfine-not-thin.md) [pyqtgraph][tick-density][bottom-ticks][target-count][min-gap][fft-frequency-axis][right-edge-truncation] — Target-count nice-tick fitters must REJECT a candidate on interior min-gap collision (not thin it); thinning let an over-fine 0.01 step win on count and truncate the FFT/heatmap X axis short of the right edge. `_apply_target_bottom_ticks` used `continue`; sibling `_fit_x_tick_labels` already `return None`s (why time-domain was fine). Keep thinning only in `extreme_narrow`.
- [responsive-pane-containers](pyqt-ui/2026-04-24-responsive-pane-containers.md) [layout][inspector][navigator][wide-pane] — Use scroll/splitters; verify wide pane (1.5×~3× default) so Expanding children stay capped.
- [flush-after-axis-mutation-not-before](pyqt-ui/2026-04-25-flush-after-axis-mutation-not-before.md) [debounce][qtimer][xlim-changed][ordering] — Drain debounced refresh AFTER the set_xlim/set_ylim mutation that re-schedules it; use try/finally for one-call-site coverage of all return paths.
- [cache-invalidation-event-conditional](pyqt-ui/2026-04-25-cache-invalidation-event-conditional.md) [cache][qtimer][handler-replay] — Invalidate state-derived caches on a last-value diff, not on handler entry; QTimer.singleShot(0, handler) replays will otherwise wipe the cache every tick.
- [matplotlib-axes-callbacks-lifecycle](pyqt-ui/2026-04-25-matplotlib-axes-callbacks-lifecycle.md) [matplotlib][callbacks][lifecycle] — Axes.callbacks survive fig.clear(); store cids and disconnect-before-rebuild, otherwise callbacks accumulate or wire to dead axes.
- [qthread-wait-deadlocks-queued-quit](pyqt-ui/2026-04-25-qthread-wait-deadlocks-queued-quit.md) [qthread][worker][signal-slot][deadlock][testing] — `thread.wait()` blocks the main loop; AutoConnection + queued thread.quit deadlocks; use Qt.DirectConnection for thread-safe slots in standalone tests.
- [tightbbox-survives-offscreen-qt](pyqt-ui/2026-04-25-tightbbox-survives-offscreen-qt.md) [matplotlib][bbox][export][clipboard][offscreen][hidpi][scale][grab] — bbox crop primary w/ degenerate-rect fallback under offscreen Qt; for hi-DPI scaled render, grab() is the realizability probe (not render()), keep 1×1 fallback un-scaled, cap the factor.
- [defer-retry-from-worker-failed-slot](pyqt-ui/2026-04-25-defer-retry-from-worker-failed-slot.md) [qthread][worker][retry][qtimer][re-entry-guard] — Synchronous retry from a queued worker-failed handler hits the host's isRunning re-entry guard; defer with QTimer.singleShot(0) so thread.finished clears the ref first.
- [conditional-visibility-init-sync-and-paired-field-children](pyqt-ui/2026-04-26-conditional-visibility-init-sync-and-paired-field-children.md) [visibility][init][qformlayout][paired-field][isHidden][inspector][reparent][shared-widget][per-mode-state][chk_range] — Conditional-visibility helpers must seed at __init__ end + propagate setVisible to paired-field children; a SHARED reparented chk_range carries ONE flag for all modes, so snapshot-old/restore-new per mode at the reparent point (blockSignals) to stop FFT-drag leaking into Time-Domain.
- [action-button-on-group-title-needs-qframe-header](pyqt-ui/2026-04-26-action-button-on-group-title-needs-qframe-header.md) [groupbox][title][header][action-button][layout][inspector][wa-styledbackground] — Use QFrame[label+stretch+button] for inline-action titles; pair every wrapper QFrame with a QSS objectName rule or QFrame{bg:#fff} re-polishes white.
- [inspector-content-max-width-and-tinted-card-bleed](pyqt-ui/2026-04-26-inspector-content-max-width-and-tinted-card-bleed.md) [layout][inspector][splitter][qframe][wa-styledbackground][max-width][rework-risk] — Splitter-pane content must cap maxWidth + left-anchor; wrapper QFrames need paired QSS; setFixedSize on tool buttons needs scoped QSS to escape global QPushButton sizing.
- [popover-accept-deactivate-race](pyqt-ui/2026-04-26-popover-accept-deactivate-race.md) [qdialog][accept][reject][windowdeactivate][race][idempotency][popover][focus-out][offscreen] — QDialog.done is not idempotent; auto-reject on WindowDeactivate after accept silently flips result. Add _is_closing guard in accept/reject and drive the race manually in offscreen tests.
- [popup-clearfocus-needs-strongfocus-on-popup-itself](pyqt-ui/2026-04-27-popup-clearfocus-needs-strongfocus-on-popup-itself.md) [popup][focus-out][clearfocus][focus-policy][testing][offscreen] — QWidget.clearFocus() is a no-op when the widget does not own focus; testable focus-out popups must give the popup frame StrongFocus and setFocus(popup) before delegating to inner controls.
- [modal-from-qthread-finished-segfaults-offscreen](pyqt-ui/2026-04-27-modal-from-qthread-finished-segfaults-offscreen.md) [qmessagebox][qthread][offscreen][segfault][testing][isvisible] — QMessageBox opened from a QThread.finished slot under qtbot.waitUntil on Windows offscreen segfaults; gate toasts on self.isVisible() so hidden test dialogs skip the modal.
- [qss-padding-overrides-setcontentsmargins](pyqt-ui/2026-04-27-qss-padding-overrides-setcontentsmargins.md) [qss][stylesheet][contentsmargins][qgroupbox][qformlayout][polish] — Global QSS padding wins over Python setContentsMargins; inline per-widget setStyleSheet with long-form padding-left/right/bottom is the survivable override. QFormLayout label columns also need cross-form unification.
- [qss-subcontrol-needs-explicit-arrow-glyph](pyqt-ui/2026-04-28-qss-subcontrol-needs-explicit-arrow-glyph.md) [qss][stylesheet][subcontrol][arrow][affordance][qtawesome] — Partially styling a subcontrol (drop-down/up-button/down-button) suppresses Qt's default arrow; declare ::down-arrow/::up-arrow with an explicit image:url glyph (qtawesome→PNG cache, forward-slash paths) and a faint resting tint so spinbox/combobox gutters are visible at rest.
- [save-action-must-not-close-gating-modal](pyqt-ui/2026-05-15-save-action-must-not-close-gating-modal.md) [qdialog][modal][gating][save-action][reachability][review-modal][test-backdoor] — When a save action gates a sibling button on the same modal, don't auto-close; use an inline status label and let the user click the now-enabled action. Tests that flip private gate flags are a smell.
- [qmessagebox-static-warning-hangs-offscreen](pyqt-ui/2026-05-19-qmessagebox-static-warning-hangs-offscreen.md) [qmessagebox][offscreen][modal][testing][headless][qt-platform] — `QMessageBox.warning/information/critical` static helpers call exec() and deadlock under QT_QPA_PLATFORM=offscreen; drive an instance with `setWindowModality(WindowModal) + .open()` and keep a `self._*_box` reference alive.
- [mpl-event-coupled-tests-survive-renderer-swap](pyqt-ui/2026-05-28-mpl-event-coupled-tests-survive-renderer-swap.md) [renderer-swap][matplotlib][pyqtgraph][test-coupling][production-consumers][getattr-guarded-dead-feature][toolbar-branch-flip][register-replot-callback-gate] — Renderer swap: grep tests AND production for the canvas's mpl attribute surface (`fig`, `draw_idle`, `full_reset`, `axes_list`); unguarded consumers crash, getattr-guarded ones (PgNavigationToolbar home/pan/zoom) go silently inert. A FigureCanvas→pg swap also FLIPS the card's toolbar branch (mpl NavigationToolbar→PgNavigationToolbar); give fixed-layout pg canvases `axes_list` shims + `reset_view_to_data_extents` — exercise live buttons. The card gates history+mode re-bind on `register_replot_callback` PRESENCE, so add it + fire `_run_replot_callbacks()` each rebuild + a `_channel_lines` `_HistoryHandle` (time handle's `set_ylim` no-op) or back/forward stays dead.
- [arraytoqpath-not-byte-identical-to-moveto-lineto-loop](pyqt-ui/2026-05-28-arraytoqpath-not-byte-identical-to-moveto-lineto-loop.md) [qpainterpath][arraytoqpath][pyqtgraph][perf][vectorize][nan-gap] — `arrayToQPath` matches a `moveTo/lineTo` loop ONLY for all-finite n>=2; NaN gaps and single points diverge, so gate the vectorized build and keep the loop as fallback, locked by an element-coord parity test.
- [pyqtgraph-axisitem-setwidth-clamp-and-builtin-right-column-spacing](pyqt-ui/2026-05-29-pyqtgraph-axisitem-setwidth-clamp-and-builtin-right-column-spacing.md) [pyqtgraph][axisitem][setwidth][overlay][grid][setHorizontalSpacing] — `AxisItem.setWidth` is a HARD clamp not a floor; overlay `showGrid(y)` lights both built-in axes (use x-only); `setHorizontalSpacing` skips the built-in right-axis column, so route every right channel through contiguous appended columns.
- [devicecoordinatecache-no-win-on-overlapping-viewboxes](pyqt-ui/2026-05-31-devicecoordinatecache-no-win-on-overlapping-viewboxes.md) [pyqtgraph][perf][devicecoordinatecache][viewbox][overlay][repaint][density-gate] — DeviceCoordinateCache speeds disjoint subplot rows ~15× but ZERO win for overlay's overlapping aux ViewBoxes; cache subplot-only → forces a TWO-budget density gate (sum-tight overlay vs max-generous subplot); time the real grab() frame.
- [splitter-setsize-requires-shown-widget](pyqt-ui/2026-05-31-splitter-setsize-requires-shown-widget.md) [splitter][visibility][offscreen][testing][widget-lifecycle] — QSplitter.setSizes is silently ignored and child isVisible() stays False unless host.show() + splitter.resize(w,h) precede the setSizes call.
- [checkindicator-subelementrect-needs-features-flag](pyqt-ui/2026-06-03-checkindicator-subelementrect-needs-features-flag.md) [qtreewidget][checkbox][hit-area][subelementrect][styleoption][offscreen][mousepress] — SE_ItemViewItemCheckIndicator returns null unless opt.features|=HasCheckIndicator + opt.checkState set; toggle via setCheckState and return without super() to avoid double-toggle.
- [dynamic-property-border-needs-styledbackground-and-padding](pyqt-ui/2026-06-04-dynamic-property-border-needs-styledbackground-and-padding.md) [qss][dynamic-property][border][focus][wa-styledbackground][padding][verify-pixels][grab-lag][tint][stale-state][additive-vs-subtractive-probe] — `[prop="true"]` border on a margin-0 container needs WA_StyledBackground + padding (+tint halo); trust saved PNG over a pixel probe — and an ADDITIVE stale banner over DIMMED curves INVERTS a region non-white count, so assert object state (opacity/text) not aggregate ink.
- [colorbaritem-label-axis-and-silent-setlevels](pyqt-ui/2026-06-11-colorbaritem-label-axis-and-silent-setlevels.md) [pyqtgraph][colorbaritem][heatmap][setlevels][scene-rect][colormapmenu][colormapmenu-false] — label= on LEFT axis; setLevels emits nothing; insert_in puts the bar INSIDE plot.sceneBoundingRect (guard by extents); bar's ColorMapMenu ignores host setMenuEnabled but IS killed by ColorBarItem(colorMapMenu=False) at construction.
- [inspector-tick-counts-vs-pg-density-factors](pyqt-ui/2026-06-11-inspector-tick-counts-vs-pg-density-factors.md) [pyqtgraph][tick-density][inspector-contract][unit-mismatch] — set_tick_density takes tick COUNTS (10/10) not pg density factors; conversion is inline in TickDensityController; verify knob at extremes (step quantization).
- [sigmouseclicked-fires-after-viewbox-menu](pyqt-ui/2026-06-11-sigmouseclicked-fires-after-viewbox-menu.md) [pyqtgraph][sigmouseclicked][context-menu][viewbox][event-order] — ev.accept() in a sigMouseClicked slot cannot block the ViewBox menu (emitted after item dispatch); gate via vb.setMenuEnabled or raiseContextMenu override.
- [qthread-terminate-noop-on-gil-bound-macos](pyqt-ui/2026-06-11-qthread-terminate-noop-on-gil-bound-macos.md) [qthread][terminate][qfatal][macos][gil] — terminate() never lands on GIL-held numpy workers on macOS (Windows-only backstop); calibrate stuck jobs ≥10× wait budget, reproduce exit-134 on old code first.
- [qgraphicslayoutitem-destructor-self-heals-layout-count](pyqt-ui/2026-06-11-qgraphicslayoutitem-destructor-self-heals-layout-count.md) [qgraphicsgridlayout][destructor][mutation-testing][teardown] — ~QGraphicsLayoutItem self-removes from its parent layout when the item dies, making a lone layout.removeItem unfalsifiable; mutate the teardown GUARD, not the call.
- [slice-must-read-same-display-matrix-as-heatmap](pyqt-ui/2026-06-11-slice-must-read-same-display-matrix-as-heatmap.md) [pyqtgraph][heatmap][slice][display-space][vmin-vmax][db] — Convert dB ONCE in plot_result, hand the display matrix + explicit vmin/vmax to plot_or_update_heatmap(amplitude_mode='amplitude'); the slice reads _matrix_disp so letting the heatmap re-convert silently desyncs slice vs image levels.
- [shared-cell-picker-must-be-result-aware-not-global](pyqt-ui/2026-06-11-shared-cell-picker-must-be-result-aware-not-global.md) [pyqtgraph][heatmap][hover][value-at][argmin][floor-fraction][db][unit][caliber] — Hover & remark on one canvas must share a cell-picker that branches on whether axis arrays exist (argmin w/ result, floor-fraction for Order) — don't flip the global picker; and dB-mode readouts label 'dB', not result.unit.
- [cache-key-stability-id-reuse-and-param-roundtrip](pyqt-ui/2026-06-11-cache-key-stability-id-reuse-and-param-roundtrip.md) [cache][lru][id-reuse][memo][round-trip][get-params][apply-params][cache-key][multiview] — id() memo keys go stale once an LRU frees+reuses the object (stamp a monotonic token); and a get/apply-asymmetric param (FFT overlap fraction vs percent) silently desyncs the key on view switch — drop phantom fields the compute never reads.
- [heatmap-split-queue-and-capture-clobbers-focused-pane](pyqt-ui/2026-06-11-heatmap-split-queue-and-capture-clobbers-focused-pane.md) [qthread][queue][sequential][multiview][heatmap][pane][capture][source-routing] — Section sequential queue reuses ONE worker; render onto job's stored pane_idx, clear refs before pumping next + clear queue in closeEvent; capture-on-compute overwrites the FOCUSED pane's source from the inspector, so wire focused-pane sources via combos (incl combo_rpm) and set only non-focused panes on state.
- [locked-levels-dual-path-and-toggle-edge-vs-nonedge](pyqt-ui/2026-06-11-locked-levels-dual-path-and-toggle-edge-vs-nonedge.md) [pyqtgraph][heatmap][colorbar][setlevels][levels-changed][multiview][compare-toggle][edge-signal][blocksignals][write-back][dual-path] — Split level-lock by layer (page=canvas↔canvas mirror, host=canvas→inspector echo gated on focused pane); blockSignals the silent setLevels mirror anyway; disconnect-before-connect re-locks; emit a NEW edge off toggled(bool) not a non-edge legacy signal, seed state→button under a suppress flag, count handler not final value for multi-connect.
- [split-combined-export-property-lookup-and-timedomain-view-pairing](pyqt-ui/2026-06-11-split-combined-export-property-lookup-and-timedomain-view-pairing.md) [export][grab-pixmap][combined-split][multiview][property-vs-method][view-pairing][time-domain][verify-ui-visually][copy-image][page-for-mode] — Host's page_for_mode is a @property (returns dict), not a method — wiring it as page_for_mode() crashes only on the LIVE copy click; and the time-domain split renders a DIFFERENT view in the secondary (view-pairing), so a harness using enter_split()+plot_time() grabs a false-blank — drive split_requested and re-grab both panes for non-white content.
- [left-drag-region-select-overrides-pan-and-menu-append-after-reorder](pyqt-ui/2026-06-14-left-drag-region-select-overrides-pan-and-menu-append-after-reorder.md) [pyqtgraph][viewbox][mousedragevent][linearregionitem][frame-select][context-menu][reorder][dual-emit][sigrangechangedmanually] — Override mouseDragEvent (return early on LeftButton+axis None, super() otherwise) to swap pan→region frame-select; the range then reaches main_window via a DUAL path (drag→region emit vs pan/wheel→viewRange emit); append custom menu actions AFTER redesign_pg_context_menu's fixed-whitelist reorder; keep region out of grab_pixmap.
- [boundary-grid-suppression-and-stacked-left-axis-unify](pyqt-ui/2026-06-14-boundary-grid-suppression-and-stacked-left-axis-unify.md) [pyqtgraph][axisitem][generateDrawSpecs][grid][double-line][setWidth][stacked-plots][left-axis-align][single-pane][setGrid][top-right-axis] — Subclass AxisItem.generateDrawSpecs + drop the boundary tickSpec (~1.5px from rect edge) on left/bottom, AND setGrid(False) on top/right (plain axes re-draw the suppressed boundary) — both halves needed; shared context_menu showGrid re-lights top/right; unify single-pane left axes via setWidth(max), left-first then right-align.
- [eliding-label-stable-anchor-and-text-returns-elided](pyqt-ui/2026-06-15-eliding-label-stable-anchor-and-text-returns-elided.md) [layout][footer][hint-bar][elide][qsizepolicy][qlabel][anchor][rotation][stable-placement][centered-group][separator-visibility] — Footer row: left-anchored = Ignored+ElideRight first item; CENTERED group = symmetric stretches + Maximum hug-container with the eliding child flipped to Preferred (Ignored collapses to 0 when centered); toggle a ` · ` separator on discovery-text presence; QLabel.text() returns ELIDED — read full_text().
- [module-constants-need-defs-module-in-mixin-package](pyqt-ui/2026-06-18-module-constants-need-defs-module-in-mixin-package.md) [package-split][mixin][constants][circular-import][monkeypatch] — Module-level constants from a QMainWindow monolith must go in a neutral `_defs.py` (no intra-package imports) to avoid circular imports when mixins and window.py both reference them; re-export from `__init__.py` for backward compat.
- [display-param-guard-vs-preset-load](pyqt-ui/2026-06-21-display-param-guard-vs-preset-load.md) [apply_params][preset][weighting][partial-dict][display-param][colorbar] — Both `apply_params` AND `_apply_preset_values` must guard optional keys with `if 'key' in d:` (Task 6 correction: `.get(key,'None')` in `_apply_preset_values` resets A-weighting on legacy preset load); display-only params go in render sig + `get_params` but NOT cache keys.
- [heatmap-auto-level-absolute-vs-relative](pyqt-ui/2026-06-21-heatmap-auto-level-absolute-vs-relative.md) [pyqtgraph][heatmap][colorbar][levels][z_auto][absolute-db][peak-relative][write-back][blocksignals][auto-manual-jump] — Absolute-dB heatmap paths must use fixed-span [peak−SPAN, peak], not z_floor/z_ceiling as peak offsets; write the result back to inspector spins under blockSignals so auto→manual is jump-free.

- [tooltip-caliber-annotation-vs-numeric-change](pyqt-ui/2026-06-21-tooltip-caliber-annotation-vs-numeric-change.md) [tooltip][caliber][welch][rms][characterization-test][user-facing-text] — Algorithm caliber gap (RMS vs peak) → tooltip annotation not numeric change; characterize dB offset in tests; verify via toolTip() not source inspection.
- [companion-curve-shares-source-axis-not-new-row](pyqt-ui/2026-06-22-companion-curve-shares-source-axis-not-new-row.md) [pyqtgraph][time-domain][filter-overlay][companion-curve][dash][setpen][emphasis][channel-lines][subplot-row][axis-ownership][live-toggle][setvisible] — Overlay a paired (dashed) curve on its source's axis: bind onto the source PgAxisHandle (reuse add_line_item), register under its own name + a _companion_names set, but DON'T append to axes_list (subplot count == source count); _apply_pdi_emphasis's mkPen(color,width) SILENTLY resets DashLine→SolidLine so read+pass pen.style(); carry pairing via optional 8th meta dict + grep fixed-arity unpacks. 06-23: the AXIS belongs to the CHANNEL not the original line — build it when original_visible OR has-visible-companion (else 显示原始 off + 滤波 on goes blank); always add original line but setVisible(flag). Live 秒生效 toggles get dedicated *_visibility_changed signals → set_*_lines_visible walks _channel_lines + setVisible + draw, NOT filter_changed.
- [narrow-y-overlay-cost-is-stroke-count-not-data](pyqt-ui/2026-06-22-narrow-y-overlay-cost-is-stroke-count-not-data.md) [pyqtgraph][perf][overlay][envelope][bucket-count][narrow-y][raster-fill][y-clip][aa-flip-back][headroom-mult] — Narrow-Y overlay paint is raster fill linear in envelope BUCKET count (displayed pts flat, cost 4–5×); Y-clip is a no-op, cap per-curve buckets by channel count (overlay-only) + feed effective width into the cache key. CAP must be `×K/(2N)`, K=1.3: bare `//(2N)` lands summed pts AT the AA-off threshold so the gate's `metric>off_budget` re-enables AA in dense overlay. Distinct from AA-compositing cost.
- [paintevent-hook-needs-class-level-override](pyqt-ui/2026-06-23-paintevent-hook-needs-class-level-override.md) [pyqtgraph][paintevent][qt-virtual-dispatch][class-swap][graphicsview][raster-timing] — Timing a paint frame needs a CLASS-level paintEvent override via runtime `__class__` swap on the live GraphicsView (`_glw`); instance-attr assignment / viewport swap are never called (Qt C++ virtual dispatch). Event filter proves delivery only; offscreen the heavy dense frame fires DEFERRED after the loop (grab() is a cached blit) — log each paint with an absolute timestamp.
- [subplot-dense-cap-must-hit-initial-bind-not-just-refresh](pyqt-ui/2026-06-23-subplot-dense-cap-must-hit-initial-bind-not-just-refresh.md) [pyqtgraph][perf][subplot][envelope][bucket-count][dense-channel][raster-fill][initial-bind][show-original][setvisible] — 显示原始 re-show jank is deferred path-regen on the FIRST paint after setVisible (NOT recompute — spy positions_envelope: 0 calls; draw() is async). Extend the bucket cap to subplot for ≥2 high-density channels, but apply it at `_initial_bind_pixel_width` too — capping only `_refresh_visible_data` misses the build (range-key gate no-ops the first refresh). 31992→10392 pts, first-repaint 217→103ms. Time `viewport.repaint()` (offscreen grab() is a cached blit ≈1ms, hides the wall).

## refactor

- [cross-layer-constant-promote-to-package-root](refactor/2026-04-22-cross-layer-constant-promote-to-package-root.md) [layering][constants][dependency-rules] — When two layers forbidden from importing each other share a constant, hoist it to the package root, do not duplicate it.
- [default-fallback-must-be-observable](refactor/2026-05-19-default-fallback-must-be-observable.md) [defaults][fallback][observability][silent-failure][ui-contract][backend][dependency-injection] — `dep or FakeDep()` is safe only when the fallback path emits a visible signal naming why the primary was unreachable; gate auto-swap on `isinstance(_, FallbackType)` so injected backends survive.
- [monkeypatch-anchor-survives-module-to-package](refactor/2026-06-18-monkeypatch-anchor-survives-module-to-package.md) [arch][import-cycle][monkeypatch][package-split][same-named-package] — Splitting `X.py` → `X/` breaks monkeypatch targets: re-exporting `QMenu` in `__init__.py` copies the binding, so `presets.QMenu` stays unpatched. Fix: runtime `sys.modules.get(pkg_name).QMenu` lookup at call time in the sub-file method, not a top-level import.
- [mpl-canvas-retirement-test-blast-radius](refactor/2026-06-18-mpl-canvas-retirement-test-blast-radius.md) [arch][dead-code][test-blast-radius][matplotlib][renderer-swap][canvases] — Retiring a canvas class touches far more test files than the plan names; grep ALL tests for `ClassName` + `inspect.getsource` before deleting.
