"""Decomposed helpers for the pyqtgraph time-domain canvas.

During the decomposition phases, ``TimeDomainCanvasPG`` remains in the legacy
``mf4_analyzer.ui.pg_canvases`` module while low-coupling helpers live here.
The legacy module re-exports the moved names so existing imports keep working.
"""
